import { Component, OnInit } from '@angular/core';

@Component({
    selector: 'network-top',
    moduleId: module.id,
    templateUrl: 'network.component.html'
})

export class NetworkComponent implements OnInit{ 
    ngOnInit(){
        
    }

}